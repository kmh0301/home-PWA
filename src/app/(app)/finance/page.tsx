export default function FinancePage() {
  return (
    <section className="rounded-[28px] bg-[var(--color-panel)] px-5 py-6">
      <p className="text-xs font-semibold uppercase tracking-[0.24em] text-[var(--color-muted)]">
        Finance placeholder
      </p>
      <h2 className="mt-2 text-2xl font-semibold text-[var(--color-foreground)]">
        Finance routes start in Phase 1 and Phase 2
      </h2>
      <p className="mt-3 text-sm leading-6 text-[var(--color-muted)]">
        This route exists so the protected shell is structurally complete before real
        finance flows and AI entrypoints are implemented.
      </p>
    </section>
  );
}
