---
phase: 01-secure-foundation-and-household-boundary
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/lib/time/hong-kong.ts
  - src/lib/time/hong-kong.testable.ts
  - supabase/verify/phase-1-household-isolation.sql
  - docs/plans/phase-1-foundation-verification.md
autonomous: true
requirements:
  - AUTH-02
  - HOUS-04
user_setup: []
must_haves:
  truths:
    - "Shared day and month boundary calculations are anchored to Asia/Hong_Kong in one reusable utility."
    - "Phase 1 has an explicit verification path for auth redirects, household membership branching, and cross-household denial."
    - "Later finance and chore work can reuse the same Hong Kong time rules instead of re-implementing date math ad hoc."
  artifacts:
    - path: src/lib/time/hong-kong.ts
      provides: "Reusable Hong Kong time boundary helpers for day, week, and month calculations"
      min_lines: 30
    - path: supabase/verify/phase-1-household-isolation.sql
      provides: "Repeatable SQL checks proving Phase 1 household isolation expectations"
      min_lines: 25
    - path: docs/plans/phase-1-foundation-verification.md
      provides: "Phase-specific verification checklist covering auth, routing, membership, and time-boundary checks"
      min_lines: 30
  key_links:
    - from: docs/plans/phase-1-foundation-verification.md
      to: supabase/verify/phase-1-household-isolation.sql
      via: "Verification guide points to executable database checks"
      pattern: "phase-1-household-isolation\\.sql"
    - from: docs/plans/phase-1-foundation-verification.md
      to: src/lib/time/hong-kong.ts
      via: "Verification instructions cover HKT edge cases and helper usage"
      pattern: "hong-kong\\.ts|Asia/Hong_Kong"
---

<objective>
Add the shared Hong Kong time groundwork and the explicit verification artifacts that prove the Phase 1 foundation is trustworthy.

Purpose: the roadmap goal includes `Asia/Hong_Kong` time rules and verified household isolation. This plan makes both reusable and testable so later phases can inherit them instead of reconstructing boundary logic or relying on memory.
Output: a reusable Hong Kong time helper module, a repeatable SQL verification script for household isolation, and a phase-specific verification checklist that ties auth, membership, and time checks together.
</objective>

<execution_context>
@/Users/manheiko/.codex/get-shit-done/workflows/execute-plan.md
@/Users/manheiko/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation-and-household-boundary/01-RESEARCH.md
@.planning/phases/01-secure-foundation-and-household-boundary/01-VALIDATION.md
@src/lib/onboarding/state.ts
@docs/plans/DB-SCHEMA.sql
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create reusable Hong Kong boundary helpers for downstream finance and chore work</name>
  <files>src/lib/time/hong-kong.ts, src/lib/time/hong-kong.testable.ts</files>
  <action>Implement a small utility module that makes `Asia/Hong_Kong` day, week, and month boundaries explicit. Keep it server-safe and deterministic, with helper entry points that can be reused by settlement math, recurrence logic, and date-label formatting later. Add a narrow testable surface or fixture helper so boundary cases near HKT midnight and month rollover can be checked without bringing in a full test framework.</action>
  <verify>Run `npx tsc --noEmit` and manually exercise representative UTC timestamps around HKT midnight and month-end using the exported helper functions.</verify>
  <done>The repo has one reusable Hong Kong time source of truth and its edge cases are documented enough for later phases to consume safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable household-isolation verification SQL</name>
  <files>supabase/verify/phase-1-household-isolation.sql</files>
  <action>Create a SQL verification script that seeds or assumes two households and proves the current authenticated actor can only see their own `households`, `household_members`, `household_invites`, and `payment_accounts` rows. Include both positive checks for same-household access and negative checks for cross-household denial, using the functions and RLS rules landed in Plan 01-02.</action>
  <verify>Run the SQL file against the reconciled Supabase database and capture the expected zero-row or permission-denied outcomes for foreign-household access attempts.</verify>
  <done>There is a repeatable database-level proof of household isolation that can be rerun before later phases touch more household-scoped tables.</done>
</task>

<task type="auto">
  <name>Task 3: Publish a single Phase 1 verification checklist</name>
  <files>docs/plans/phase-1-foundation-verification.md, supabase/verify/phase-1-household-isolation.sql, src/lib/time/hong-kong.ts</files>
  <action>Write a concise verification guide that ties together the lint/typecheck commands, signed-out and signed-in routing checks, no-household versus has-household entry behavior, household-isolation SQL, and Hong Kong time-boundary inspection steps. Make it explicit which checks are automated, which are manual, and which plan artifacts they depend on.</action>
  <verify>Cross-check the guide against `01-VALIDATION.md` and ensure every manual-only verification listed there now has a concrete artifact or runbook step.</verify>
  <done>Phase 1 can be re-verified end-to-end from one document without relying on tribal knowledge.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] Hong Kong boundary helper returns expected values for sample timestamps around HKT midnight and month rollover
- [ ] Household isolation SQL produces expected same-household success and foreign-household denial results
- [ ] Verification guide covers signed-out, no-household, and active-household routing outcomes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The phase now has reusable Hong Kong time rules and an explicit, rerunnable verification path for auth and household isolation
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation-and-household-boundary/01-03-SUMMARY.md`
</output>
