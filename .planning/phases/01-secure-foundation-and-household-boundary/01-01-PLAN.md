---
phase: 01-secure-foundation-and-household-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - src/app/page.tsx
  - src/app/(app)/layout.tsx
  - src/app/onboarding/layout.tsx
  - src/app/auth/callback/route.ts
  - src/lib/supabase/middleware.ts
  - src/lib/auth/session.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "New users can sign up and transition into the same trusted authenticated session flow as returning users."
    - "Unauthenticated visitors are redirected away from protected household surfaces before app content renders."
    - "Authenticated users can reopen the app into a valid Supabase session and land on the correct entry route instead of being stranded on auth pages."
    - "Public auth routes stop acting as alternate app entry points once a valid session exists."
  artifacts:
    - path: middleware.ts
      provides: "Protected-route enforcement and auth-route redirect behavior"
      min_lines: 25
    - path: src/app/(auth)/login/actions.ts
      provides: "Sign-up and sign-in actions that feed the trusted auth flow"
      min_lines: 20
    - path: src/lib/auth/session.ts
      provides: "Shared server-side session and current-user resolution for app routing"
      min_lines: 25
    - path: src/app/page.tsx
      provides: "Single app entry redirect that uses trusted auth state"
      min_lines: 10
  key_links:
    - from: middleware.ts
      to: src/lib/supabase/middleware.ts
      via: "Session refresh and cookie propagation"
      pattern: "updateSession"
    - from: src/app/page.tsx
      to: src/lib/auth/session.ts
      via: "Server-side entry routing"
      pattern: "get.*Session|get.*CurrentUser"
---

<objective>
Harden the auth and route boundary so Phase 1 has one trusted session-aware entry path into the product.

Purpose: AUTH-01 and AUTH-02 depend on request-time enforcement, not just login screens, because later household and finance work assumes protected surfaces never render for anonymous traffic.
Output: tightened middleware matchers, a shared server-side session helper, and app entry/auth callback redirects that behave correctly for signed-out and signed-in users.
</objective>

<execution_context>
@/Users/manheiko/.codex/get-shit-done/workflows/execute-plan.md
@/Users/manheiko/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation-and-household-boundary/01-RESEARCH.md
@.planning/phases/01-secure-foundation-and-household-boundary/01-VALIDATION.md
@middleware.ts
@src/app/page.tsx
@src/app/(app)/layout.tsx
@src/app/onboarding/layout.tsx
@src/app/(auth)/login/actions.ts
@src/app/auth/callback/route.ts
@src/lib/supabase/middleware.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Lock sign-up, sign-in, and callback flow into one trusted auth contract</name>
  <files>src/app/(auth)/login/actions.ts, src/app/(auth)/login/page.tsx, src/app/auth/callback/route.ts</files>
  <action>Audit the current registration, login, and auth callback flow so `AUTH-01` is explicitly covered in this phase. Keep sign-up as a first-class path, confirm callback/session handoff feeds the same trusted app entry behavior as returning-user login, and avoid optimizing the auth boundary only for existing accounts. Stay within authentication correctness; do not add profile or onboarding feature scope.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and manually confirm a new-user sign-up path still leads into the authenticated app routing flow.</verify>
  <done>Registration, login, and callback handling are explicitly part of the phase plan and support the same trusted post-auth session flow.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten middleware and callback routing around the authenticated app boundary</name>
  <files>middleware.ts, src/app/auth/callback/route.ts, src/lib/supabase/middleware.ts</files>
  <action>Expand route protection from the current narrow matcher to the real authenticated product surfaces used in this repo, while keeping only explicit auth endpoints public. Preserve the `next` target on signed-out redirects, keep Supabase cookie refresh intact, and make the auth callback route hand off into the same trusted app entry path instead of hard-coding a dashboard assumption.</action>
  <verify>Run `npm run lint` and manually check signed-out access to `/`, `/dashboard`, `/finance`, and `/onboarding/create` redirects to `/login` with the expected next target.</verify>
  <done>Middleware blocks anonymous access to protected app surfaces, auth-only routes stay public, and callback/session refresh behavior is still valid.</done>
</task>

<task type="auto">
  <name>Task 3: Add a shared server-side session helper for app entry decisions</name>
  <files>src/lib/auth/session.ts, src/app/page.tsx, src/app/(app)/layout.tsx, src/app/onboarding/layout.tsx</files>
  <action>Create a small server helper that centralizes current-session and current-user lookup for route decisions. Use it from the root app entry and align layout-level redirect checks around it so the first redirect no longer depends on ad hoc logic. Keep the API narrow: current user, current session presence, and redirect-safe helpers only.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and confirm the helper is imported by the actual app entry/layout surfaces without duplicate auth-fetch patterns.</verify>
  <done>There is one reusable server-side session utility, and the root/app/onboarding entry points consume it for auth-aware redirect decisions.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] A new-user sign-up path remains functional and feeds the authenticated session flow
- [ ] Signed-out route checks confirm redirects from `/`, `/dashboard`, `/finance`, and `/onboarding/create`
- [ ] Signed-in route checks confirm `/login` and `/auth/callback` no longer act as alternate app homes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sign-up and login are both explicitly covered by the plan
- Session-aware routing is centralized enough that later household resolution can plug in without reworking middleware again
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation-and-household-boundary/01-01-SUMMARY.md`
</output>
