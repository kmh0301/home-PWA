---
phase: 01-secure-foundation-and-household-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260313000000_phase_0_foundation.sql
  - src/types/database.types.ts
  - src/lib/onboarding/state.ts
  - src/lib/household/current-household.ts
  - src/app/onboarding/actions.ts
autonomous: true
requirements:
  - AUTH-01
  - HOUS-04
user_setup: []
must_haves:
  truths:
    - "Current household context is resolved from authenticated membership records, not from client-supplied household identifiers."
    - "Household-scoped reads and writes for the Phase 1 tables are denied when the authenticated user is outside that household."
    - "The app and generated database types agree on the household/auth foundation that later phases will build on."
  artifacts:
    - path: supabase/migrations/20260313000000_phase_0_foundation.sql
      provides: "Executable schema, helper functions, and RLS baseline for auth/household foundation tables"
      min_lines: 80
    - path: src/lib/household/current-household.ts
      provides: "Trusted server-side household membership resolver"
      min_lines: 25
    - path: src/types/database.types.ts
      provides: "Regenerated Supabase database contract for the reconciled schema"
      min_lines: 50
  key_links:
    - from: src/lib/household/current-household.ts
      to: src/lib/onboarding/state.ts
      via: "Shared household resolution used by route decisions"
      pattern: "current-household|getCurrentHousehold"
    - from: supabase/migrations/20260313000000_phase_0_foundation.sql
      to: src/types/database.types.ts
      via: "Supabase type generation after schema reconciliation"
    - from: src/app/onboarding/actions.ts
      to: src/lib/household/current-household.ts
      via: "Server actions consume trusted membership context instead of raw household ids"
      pattern: "getCurrentHousehold|requireCurrentHousehold"
---

<objective>
Reconcile the executable Supabase foundation and add a trusted household resolver so household isolation is enforced by membership, not page parameters.

Purpose: HOUS-04 is the security center of this phase. Later features must inherit a household context that is proven by database membership and backed by RLS, not by loose client input or optimistic assumptions.
Output: an executable migration for the Phase 1 auth/household tables and policies, regenerated database types, and a shared server-side household resolver used by onboarding-aware server code.
</objective>

<execution_context>
@/Users/manheiko/.codex/get-shit-done/workflows/execute-plan.md
@/Users/manheiko/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation-and-household-boundary/01-RESEARCH.md
@docs/plans/DB-SCHEMA.sql
@supabase/migrations/20260313000000_phase_0_foundation.sql
@src/types/database.types.ts
@src/lib/onboarding/state.ts
@src/app/onboarding/actions.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Turn the Phase 0 placeholder migration into an executable Phase 1 foundation baseline</name>
  <files>supabase/migrations/20260313000000_phase_0_foundation.sql, src/types/database.types.ts</files>
  <action>Copy the vetted schema statements needed for this phase from `docs/plans/DB-SCHEMA.sql` into the existing migration placeholder so a fresh project can actually create the auth and household foundation. Keep scope tight: households, household_members, household_invites, payment_accounts, helper RPCs already used by onboarding, `get_user_household_ids()`, and the RLS policies those tables need. Do not pull later finance, chore, review, or notification tables into this phase just because the master schema contains them. After reconciling the migration, regenerate `src/types/database.types.ts` from Supabase so the app contract matches the executable schema.</action>
  <verify>Run the documented Supabase migration flow for this repo, then regenerate types with `supabase gen types typescript --linked > src/types/database.types.ts` and confirm `npx tsc --noEmit` still passes.</verify>
  <done>The migration is executable for Phase 1 foundation tables, helper functions/policies exist in versioned SQL, and the generated TypeScript types match that baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Add a trusted current-household resolver for server-side code</name>
  <files>src/lib/household/current-household.ts, src/lib/onboarding/state.ts</files>
  <action>Create a focused server utility that resolves the current authenticated user, the active membership row, and the trusted household id from `household_members`. Refactor `getOnboardingState()` to consume that utility instead of duplicating membership lookups inline. The resolver should expose a strict variant that throws or redirects when membership is required, plus a nullable variant for routing decisions.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and inspect that `src/lib/onboarding/state.ts` no longer performs ad hoc household resolution on its own.</verify>
  <done>Server code has one trusted place to obtain current household membership, and onboarding state now depends on that shared resolver.</done>
</task>

<task type="auto">
  <name>Task 3: Replace raw household assumptions inside onboarding server actions</name>
  <files>src/app/onboarding/actions.ts, src/lib/household/current-household.ts, src/lib/onboarding/state.ts</files>
  <action>Update onboarding actions to consume the trusted household resolver wherever household membership is required. Keep this phase scoped to foundation primitives: use the resolver to avoid raw membership assumptions and align mutations with the Phase 1 RLS contract, but do not expand into new create/join UX beyond what already exists.</action>
  <verify>Run `npm run lint && npx tsc --noEmit`, then manually exercise create/join/account-setup flows with a seeded authenticated user to confirm the actions still resolve the correct membership path.</verify>
  <done>Onboarding server actions rely on verified household membership primitives rather than scattered household id assumptions.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] Supabase migration for the Phase 1 foundation applies cleanly on a fresh or reset database
- [ ] Regenerated `src/types/database.types.ts` reflects the reconciled schema and RPC signatures
- [ ] Manual membership check confirms non-members cannot read or mutate another household through Phase 1 tables
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Household context is resolved from membership-backed server helpers and the database baseline enforces the same boundary
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation-and-household-boundary/01-02-SUMMARY.md`
</output>
