---
phase: 02-household-onboarding-and-shared-membership
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/onboarding/state.ts
  - src/app/onboarding/layout.tsx
  - src/app/onboarding/actions.ts
  - src/app/onboarding/join/page.tsx
  - src/app/onboarding/join/success/page.tsx
autonomous: true
requirements:
  - HOUS-01
  - HOUS-03
user_setup: []
must_haves:
  truths:
    - "Signed-in users without a household can access both create and join onboarding routes instead of being forced back to create."
    - "The join flow has a dedicated success milestone before payment account setup."
    - "Join preview and confirmation states align with the approved phase context: creator identity, member count, editable prefilled display name, and one-step confirmation."
  artifacts:
    - path: src/lib/onboarding/state.ts
      provides: "Central onboarding route-state helpers that support allowed route sets instead of one coarse next-route"
      min_lines: 35
    - path: src/app/onboarding/layout.tsx
      provides: "Server-side route gating that correctly allows create/join/join-success/accounts at the right lifecycle states"
      min_lines: 35
    - path: src/app/onboarding/join/success/page.tsx
      provides: "Dedicated post-join success milestone before account setup"
      min_lines: 25
  key_links:
    - from: src/app/onboarding/layout.tsx
      to: src/lib/onboarding/state.ts
      via: "Layout consumes the centralized onboarding route-set helper"
      pattern: "getNextOnboardingRoute|allow|route"
    - from: src/app/onboarding/actions.ts
      to: src/app/onboarding/join/success/page.tsx
      via: "Join claim redirects into the new dedicated success step before accounts"
      pattern: "join/success|accounts"
---

<objective>
Refactor onboarding routing and state handling so the approved create/join/join-success flow is reachable, coherent, and still server-first.

Purpose: the current route guard model breaks `/onboarding/join` for signed-in users with no household and skips the desired post-join milestone. This plan repairs the route model before the final UI polish layer.
Output: onboarding route helpers and layout logic that support valid no-household subroutes, a dedicated join-success route, and updated join action wiring into that flow.
</objective>

<execution_context>
@/Users/manheiko/.codex/get-shit-done/workflows/execute-plan.md
@/Users/manheiko/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-household-onboarding-and-shared-membership/02-CONTEXT.md
@.planning/phases/02-household-onboarding-and-shared-membership/02-RESEARCH.md
@src/app/onboarding/layout.tsx
@src/app/onboarding/actions.ts
@src/app/onboarding/join/page.tsx
@src/app/onboarding/accounts/page.tsx
@src/lib/onboarding/state.ts
@src/lib/household/current-household.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace coarse onboarding next-route logic with explicit allowed route states</name>
  <files>src/lib/onboarding/state.ts, src/app/onboarding/layout.tsx</files>
  <action>Refactor onboarding state helpers so route decisions represent allowed route sets rather than one canonical path. Preserve the current server-first redirect pattern, but make sure signed-in users with no household can legitimately remain on either create or join, and users who just joined can reach the success milestone before accounts.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and manually check route decisions for: unauthenticated user, signed-in/no-household on create, signed-in/no-household on join, signed-in/just-joined on success, signed-in/needs-accounts on accounts, and fully onboarded on dashboard. Include a two-session check that after claim both users resolve the same household identity/name through join-success and accounts.</verify>
  <done>Onboarding route enforcement no longer breaks the second-partner join path and still keeps users inside valid onboarding surfaces only.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated post-join success step and wire claim redirects through it</name>
  <files>src/app/onboarding/actions.ts, src/app/onboarding/join/success/page.tsx, src/app/onboarding/accounts/page.tsx</files>
  <action>Create the join-success page described in the phase context, carrying enough state to confirm successful household join and clearly continue into payment account setup. Update join claim redirects and any transient joined-state messaging so the accounts page is no longer the first post-claim surface.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and manually confirm a successful invite claim lands on the success page before continuing to account setup.</verify>
  <done>The second member sees a dedicated success milestone and the flow into account setup is explicit rather than abrupt.</done>
</task>

<task type="auto">
  <name>Task 3: Align join confirmation behavior with the approved preview contract</name>
  <files>src/app/onboarding/join/page.tsx, src/app/onboarding/actions.ts</files>
  <action>Update the join flow so validation output supports the approved preview card: household name, creator display name, current member count, editable display name prefilled from the authenticated account profile, and one-step confirmation. Keep the implementation route/search-param based rather than introducing a client-side onboarding store.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and manually confirm the join page shows the expected preview metadata and a prefilled display name before final confirmation.</verify>
  <done>The join UI and routing behavior match the Phase 2 context while remaining consistent with the server-rendered onboarding architecture.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] Signed-in no-household users can access both `/onboarding/create` and `/onboarding/join`
- [ ] Successful invite claim lands on a dedicated join-success step before `/onboarding/accounts`
- [ ] Join preview shows household name, creator display name, and member count
- [ ] Join display name is prefilled but editable
- [ ] Two authenticated sessions resolve the same household identity after claim and stay in that shared scope through join-success and accounts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The onboarding route model supports the real create/join success flow instead of collapsing everything into one no-household route
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-onboarding-and-shared-membership/02-02-SUMMARY.md`
</output>
