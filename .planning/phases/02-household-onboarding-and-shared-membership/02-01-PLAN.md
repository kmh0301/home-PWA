---
phase: 02-household-onboarding-and-shared-membership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260314000000_phase_2_household_onboarding.sql
  - src/app/onboarding/actions.ts
  - src/lib/household/current-household.ts
  - src/types/database.types.ts
  - supabase/verify/phase-2-household-onboarding.sql
autonomous: true
requirements:
  - HOUS-01
  - HOUS-02
  - HOUS-03
user_setup: []
must_haves:
  truths:
    - "A signed-in user can belong to at most one active household in v1, and that invariant is enforced at the database boundary."
    - "Household bootstrap is atomic: create household, creator membership, and initial invite either all succeed or all fail."
    - "Invite generation is collision-safe and never surfaces unique-code collisions as a user-facing bootstrap failure."
    - "Invite validation returns enough preview data to support the approved join confirmation UX before a claim is attempted."
    - "An owner can regenerate an expired invite with the same lifecycle guarantees as the initial bootstrap invite."
    - "Household claim and create flows fail with deterministic, user-safe error categories instead of raw database leakage."
  artifacts:
    - path: supabase/migrations/20260314000000_phase_2_household_onboarding.sql
      provides: "Schema, policy, and RPC hardening for household bootstrap, invite preview, invite regeneration, and membership uniqueness"
      min_lines: 80
    - path: src/app/onboarding/actions.ts
      provides: "Server actions that consume the hardened RPC contract, support invite regeneration, and normalize onboarding failures"
      min_lines: 40
    - path: supabase/verify/phase-2-household-onboarding.sql
      provides: "Repeatable verification for single-household membership, invite lifecycle, and two-member protection"
      min_lines: 35
  key_links:
    - from: src/app/onboarding/actions.ts
      to: supabase/migrations/20260314000000_phase_2_household_onboarding.sql
      via: "Server actions call the new household bootstrap / invite preview contract instead of stitching writes ad hoc"
      pattern: "create_household|validate_invite|claim_invite"
    - from: src/lib/household/current-household.ts
      to: supabase/migrations/20260314000000_phase_2_household_onboarding.sql
      via: "Membership lookup remains sound because v1 uniqueness is enforced in the database"
      pattern: "household_members|unique"
---

<objective>
Lock the Phase 2 household lifecycle invariants at the data and server-action boundary before any routing or UX refinement depends on them.

Purpose: later finance, chores, and notifications all assume household membership is unique, stable, and created without partial state. This plan makes the create/invite/claim contract trustworthy enough for the rest of the phase to build on.
Output: a hardened Supabase migration/RPC surface, updated server actions that use it, and a verification SQL script for onboarding edge cases.
</objective>

<execution_context>
@/Users/manheiko/.codex/get-shit-done/workflows/execute-plan.md
@/Users/manheiko/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-household-onboarding-and-shared-membership/02-CONTEXT.md
@.planning/phases/02-household-onboarding-and-shared-membership/02-RESEARCH.md
@src/app/onboarding/actions.ts
@src/lib/household/current-household.ts
@docs/plans/DB-SCHEMA.sql
@supabase/migrations/20260313000000_phase_0_foundation.sql
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce v1 household membership invariants in Supabase</name>
  <files>supabase/migrations/20260314000000_phase_2_household_onboarding.sql, src/lib/household/current-household.ts, src/types/database.types.ts</files>
  <action>Add the database-side constraints and function updates needed for Phase 2: one active household membership per user in v1, preserved two-member maximum behavior, and stable membership identity semantics. Update any generated or checked-in database types/contracts the repo relies on so server code matches the new schema and RPC payloads.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and inspect the migration logic for a clear database-enforced single-household guarantee rather than UI-only protection.</verify>
  <done>Membership uniqueness and household capacity are enforced at the persistence layer, and the TypeScript contract reflects the new shape.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ad hoc household bootstrap with an atomic server-consumable contract</name>
  <files>supabase/migrations/20260314000000_phase_2_household_onboarding.sql, src/app/onboarding/actions.ts</files>
  <action>Create or extend RPCs so household bootstrap is atomic, invite generation is collision-safe with bounded retry, invite preview returns the metadata required by the approved join UX (household name, creator display name, current member count, expiry, and validity/full-state signaling), and owners can regenerate an expired invite without breaking membership invariants. Refactor onboarding actions to use those RPCs and normalize create/validate/claim/regenerate failures into stable user-facing categories suitable for Traditional Chinese copy.</action>
  <verify>Run `npm run lint && npx tsc --noEmit` and manually trace create -> validate -> claim plus expired-invite regeneration paths to confirm there is no longer a three-write partial-state bootstrap path and invite-code collisions do not leak as user-facing failures.</verify>
  <done>The app uses one trustworthy server/database contract for household creation, preview, claim, and expired-invite regeneration, and onboarding actions no longer surface raw database messages as product copy.</done>
</task>

<task type="auto">
  <name>Task 3: Add repeatable Phase 2 verification SQL for lifecycle edge cases</name>
  <files>supabase/verify/phase-2-household-onboarding.sql</files>
  <action>Write a verification SQL script that proves the new onboarding invariants: duplicate household membership is denied, a user cannot create or claim into multiple households, two-member capacity is enforced under claim, invite rows transition from valid to used/invalid as expected, and expired invites can be regenerated into a fresh valid code without orphaning the prior lifecycle state. Keep it rerunnable so later phases can recheck the same household contract.</action>
  <verify>Run the SQL file against the project Supabase database and capture expected pass/fail outcomes for duplicate membership, full household, expired invite, regenerated invite, already-used invite, and bounded duplicate-code generation handling.</verify>
  <done>There is an executable database-level proof that Phase 2 household onboarding invariants hold beyond the UI flow.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] Migration enforces one-household-per-user and preserves two-member protection
- [ ] Create household is atomic and cannot leave orphan household/bootstrap state
- [ ] Invite generation is collision-safe with bounded retry or equivalent deterministic handling
- [ ] Invite preview returns creator and member-count data needed by the join confirmation UI
- [ ] Owners can regenerate an expired invite into a new valid code without violating membership invariants
- [ ] Verification SQL covers duplicate membership, full-household, expired-invite, regenerated-invite, and used-invite cases
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a trustworthy household lifecycle contract for downstream routing and UX work
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-onboarding-and-shared-membership/02-01-SUMMARY.md`
</output>
